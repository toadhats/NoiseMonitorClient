platform :ios, '8.0'
use_frameworks!

target 'NoiseMonitorClient' do

  pod 'SwiftDate' #date handling library
  pod 'Charts' #ios-charts graphing library
  pod 'Eureka', '~> 1.0' #interesting library that creates input forms

end
